# The bundled TRaSH Guides snapshot is generated byte for byte (backend/tools/trash_snapshot.py).
# Keep its line ends as written, so a checkout on Windows does not turn them into CRLF.
backend/app/data/*.json text eol=lf
