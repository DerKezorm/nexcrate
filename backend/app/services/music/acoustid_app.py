"""nexcrate's own AcoustID application key, shipped with the app (the owner's answer of 19.09.2026).

An application key names the application, not a person, and is public by design, as in other open tools that
fingerprint music. An installation can store its own key instead (``fingerprint.key``); that one is encrypted and wins.
Empty: nothing is shipped, and fingerprinting needs the installation's own key.
"""

APPLICATION_KEY = "xW4HKfoqIV"
